# ارزیابی پارامتر ثابت روی بازه‌های مختلف

`evaluate_params.py` فقط همان تنظیمات ورودی را اجرا می‌کند؛ جست‌وجو، تغییر پارامتر یا انتخاب بهترین تنظیمات ندارد. مقدارهای پیش‌فرض مؤثر استراتژی همراه فایل ورودی در `frozen_params.json` ثبت می‌شوند.

از پوشهٔ اصلی پروژه:

```powershell
python .\evaluate_params.py `
  --params "outputs/optimize/focused_recent_2y_50cycles/snapshots/cycles_000250/best_params.json" `
  --tests 1000 --min-months 6 --max-months 12 `
  -w 8 --output-dir "outputs/evaluate_cycles_000250_1000"
```

مسیر پوشهٔ حاوی `best_params.json` هم پذیرفته می‌شود. به‌صورت پیش‌فرض، بازه‌های تصادفی یکتا از کل تاریخ دیتاست استراتژی انتخاب می‌شوند. `--start 2024-09-01 --end 2026-09-01` محدوده را محدود می‌کند. پایان بازه exclusive است. طول بازه‌ها بر حسب ماه تقویمی است؛ `--seed 42` انتخاب آنها را تکرارپذیر می‌کند. هر بازه از موجودی اولیهٔ یکسان شروع می‌شود و warmup اندیکاتورها خارج از معاملات آن بازه است.

برای دیدن برنامه بدون اجرای بک‌تست، `--dry-run` اضافه کنید. برای ادامهٔ اجرای قطع‌شده یا بازسازی گزارش، همان فرمان را با `--resume` اجرا کنید. تغییر داده، کد، تنظیمات یا پارامترها مانع ادامهٔ checkpoint ناسازگار می‌شود. هر تست تمام‌شده فوراً در `windows/window_*.json` ذخیره می‌شود. بازه‌های خطادار در گزارش می‌مانند؛ از محاسبهٔ نسبت موفقیت حذف نمی‌شوند.

## خروجی‌ها

- `evaluation_report.xlsx`: خلاصه، معیارهای قبولی، تمام نتایج بازه‌ها، آمار توزیع معیارها، پارامترها، تنظیمات، لانگ/شورت، RSI، ورود پله‌ای، بازده ماهانه، سود معاملات و نمودار توزیع بازده.
- `window_results.csv`: همهٔ معیارهای عددی و متنی برگردانده‌شده از موتور برای هر بازه.
- `summary.json`: نتیجهٔ کلی و دلایل عبور یا رد معیارها.
- `manifest.json`، `market_data_audit.json` و `frozen_params.json`: برنامهٔ بازه‌ها، اثرانگشت داده/کد/تنظیمات، بررسی کیفیت داده و تنظیمات ثابت.
- `windows/`: خروجی خام هر تست برای بررسی و ادامهٔ اجرا.

سود معاملات، دنبالهٔ سود گزارش‌شده توسط موتور است؛ لاگ کامل زمان ورود/خروج و اجرای سفارش نیست. بازده ماهانه به شکل کسر ذخیره و در اکسل به شکل درصد نمایش داده می‌شود؛ اولین و آخرین ماه می‌توانند ناقص باشند. اطلاعات اختصاصی استراتژی تا حدی در دسترس است که موتور آن را برگرداند.

## معنی PASS و FAIL

پیش‌فرض عبور هر بازه: سود مثبت، حداقل ۵ معامله، افت سرمایه حداکثر ۳۰٪ و صفر liquidation.

پیش‌فرض PASS کلی: همهٔ تست‌ها تمام و معتبر باشند، حداقل ۷۰٪ بازه‌ها سود مثبت داشته باشند و حداقل ۷۰٪ بازه‌ها تمام شرط‌های بالا را پاس کنند. این آستانه‌ها با `--min-trades`، `--max-drawdown`، `--min-positive-ratio` و `--min-pass-ratio` قابل تنظیم‌اند؛ نسبت‌ها از صفر تا یک هستند. آستانه‌ها برای هر اجرا ثبت می‌شوند.

این نتیجه ارزیابی توصیفی پایداری است، نه `accepted=true` در research مستقل. بازه‌های تصادفی با یکدیگر هم‌پوشانی دارند؛ سودها را نباید جمع کرد یا معاملات تکراری آنها را مستقل شمرد. ستون `selection_history` بازه‌های شروع‌شده قبل از پایان انتخاب snapshot را محافظه‌کارانه `overlaps_selection` علامت می‌زند. اگر manifest مناسب نباشد، سابقه `unknown` است. `after_selection` فقط ترتیب زمانی را نشان می‌دهد، نه اثبات ندیده‌بودن داده در تمام فعالیت‌های قبلی.
