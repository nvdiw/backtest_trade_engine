# تنظیم مستقل لانگ و شورت و اجرای خودکار

فوکوس فعلی روی ۲۴ پارامتر MA و ورود مستقل لانگ/شورت است. فایل قابل ویرایش و فرمان اجرای ۱۰۰ سیکل در [راهنمای فوکوس و منابع اجرا](RUNTIME_FOCUS_GUIDE_FA.md) آمده‌اند.

## تنظیمات استراتژی اصلی MA

پارامترهای سیگنال، خروج، اندیکاتورها، اندازهٔ سفارش، اهرم، ورود پله‌ای و تنظیمات عمومی RSI اکنون برای هر جهت override مستقل دارند. کلید قدیمی بدون پیشوند، مقدار مشترک و سازگار با فایل‌های قبلی است. مقدار `null` در کلید جهت‌دار یعنی استفاده از همان مقدار مشترک؛ صفر یا `false` مقدار واقعی‌اند و به معنی ارث‌بری نیستند.

| بخش | نمونهٔ لانگ | نمونهٔ شورت | اثر |
|---|---|---|---|
| EMA سریع | `long_ema_16_period` | `short_ema_16_period` | آرایهٔ EMA و وضعیت کراس همان جهت |
| MA متوسط | `long_ma_50_period` | `short_ma_50_period` | خط مقابل EMA همان جهت |
| MA روند | `long_ma_100_period` / `long_ma_200_period` | `short_ma_100_period` / `short_ma_200_period` | تأیید و ضعف روند |
| دوره‌های اندیکاتورها | `long_period_atr` / `long_period_adx` | `short_period_atr` / `short_period_adx` | ATR، ADX، ATR-MA، RSI و میانگین حجم |
| ورود و وزن امتیازها | `long_entry_score_threshold` | `short_entry_score_threshold` | آستانه و اجزای امتیاز ورود |
| خروج | `long_exit_score_threshold` / `long_trail_retrace_pct` | `short_exit_score_threshold` / `short_trail_retrace_pct` | امتیاز خروج، trailing و محافظ‌های سود/زیان |
| فیلترها | `long_atr_filter` / `long_entry_atr_threshold` | `short_atr_filter` / `short_entry_atr_threshold` | فیلتر ATR، ADX و حجم |
| اندازه و اهرم | `long_trade_amount_percent` / `long_leverage` | `short_trade_amount_percent` / `short_leverage` | حجم سفارش و اهرم؛ tierهای `safe_leverage_*` هم جهت‌دارند |
| ورود پله‌ای | `long_scale_in_enabled` / `long_max_open_trades` | `short_scale_in_enabled` / `short_max_open_trades` | فعال‌سازی، تعداد، اندازه، trigger و کیفیت scale-in |
| وقفهٔ معامله | `long_cooldown_after_big_pnl` | `short_cooldown_after_big_pnl` | cooldown همان جهت در حالت تنظیمات مستقل |
| RSI عمومی | `long_rsi_leverage` / `long_rsi_cooldown_bars` | `short_rsi_leverage` / `short_rsi_cooldown_bars` | تنظیمات سفارش و cooldown RSI همان جهت |
| RSI از قبل جهت‌دار | `rsi_long_tp_pct` / `rsi_long_open_monthly_profit` | `rsi_short_tp_pct` / `rsi_short_open_monthly_profit` | نام‌های قبلی حفظ شده‌اند و پیشوند جدید لازم ندارند |
| فعال‌سازی جهت | `long_enabled` | `short_enabled` | فعال/غیرفعال کردن ورودهای اصلی و RSI آن جهت |

موجودی، ذخیرهٔ پول، هزینه‌های اجرا، توقف/مرکب‌سازی ماهانه و کنترل زنجیرهٔ زیان متعلق به حساب مشترک‌اند و جدا نشده‌اند. همچنین منطق قبلی اجازهٔ ورود اصلی هم‌زمان در دو جهت را نمی‌دهد. بنابراین سیگنال‌ها و تنظیمات مستقل‌اند، ولی سود یک جهت می‌تواند از طریق موجودی، توقف حساب یا اشغال پوزیشن بر معاملات بعدی جهت دیگر اثر بگذارد.

مثال JSON، مطابق EMA14/MA50 برای لانگ و EMA20/MA60 برای شورت:

```json
{
  "long_ema_16_period": 14,
  "long_ma_50_period": 50,
  "short_ema_16_period": 20,
  "short_ma_50_period": 60
}
```

برای تغییر پیش‌فرض‌ها داخل کد، دیکشنری‌های `MA_LONG_DEFAULTS` و `MA_SHORT_DEFAULTS` در `strategy_config.py` را پر کنید؛ نام‌های داخل این دو دیکشنری **بدون پیشوند جهت** نوشته می‌شوند. مثلاً `MA_SHORT_DEFAULTS = {"ema_16_period": 20, "ma_50_period": 60}`. برای مشاهدهٔ همهٔ کلیدها، `python ma_strategy.py --list-params` را اجرا کنید.

برای تست موردی نیز `--set long_ema_16_period=14 --set short_ema_16_period=20` به فرمان `ma_strategy.py` اضافه کنید. `evaluate_params.py` همان فایل‌های جهت‌دار را بدون تغییر پارامترها می‌پذیرد.

## پروفایل‌های بهینه‌سازی

| پروفایل/فرمان | پارامترهای قابل تغییر | کاربرد |
|---|---|---|
| `--profile long` | همهٔ پارامترهای لانگ | شورت روی baseline ثابت می‌ماند |
| `--profile short` | همهٔ پارامترهای شورت | لانگ روی baseline ثابت می‌ماند |
| `--profile long_signal` / `short_signal` | سیگنال همان جهت | جست‌وجوی دوره‌های MA و شرایط ورود |
| `long_exit` / `short_exit` | خروج همان جهت | بهبود مدیریت خروج |
| `long_risk_core` / `short_risk_core` | اندازه، اهرم و تعداد پوزیشن | تنظیم ریسک جهت |
| `long_rsi` / `short_rsi` | RSI همان جهت | شامل نام‌های قدیمی `rsi_long_*` / `rsi_short_*` |
| `long_scale` / `short_scale` | ورود پله‌ای همان جهت | تنظیم trigger و فیلترها |
| `--profile directional` | هر دو جهت + کنترل‌های مشترک | جست‌وجوی هم‌زمان بدون aliasهای مشترکِ بی‌اثر |
| `--auto --staged --directional` | گروه‌های فوق به‌ترتیب | تثبیت برندهٔ هر مرحله به‌عنوان baseline بعدی |
| `--autopilot` | همان Auto مرحله‌ای و جهت‌دار | فرمان کوتاه با گزارش‌های خودکار |

امتیاز انتخاب همچنان عملکرد کل حساب است؛ پروفایل `long` یا `short` حساب مجزا ایجاد نمی‌کند. برای بررسی عملکرد یک جهت به‌تنهایی، جهت دیگر را در فایل baseline غیرفعال کنید.

## شروع خودکار

```powershell
python .\optimize.py --autopilot -w 8 `
  --output-dir "outputs/optimize/directional_campaign"
```

برای شروع از فایل مشخص، `--base-source file --base-params "مسیر/best_params.json"` را اضافه کنید. برای ادامهٔ همان کمپین، همان فرمان را دوباره اجرا کنید؛ checkpoint سازگار خودکار پیدا می‌شود. حالت `--dry-run` برنامه را بدون بک‌تست نشان می‌دهد.

سیاست تاریخ فعلی Auto بر دو سال اخیر تا آخرین کندل متمرکز است؛ آن را با سیاست جداسازی دادهٔ research اشتباه نگیرید. برای حفظ دورهٔ ندیده، از ابتدا کمپینی با `--date-policy fixed` و پایان توسعهٔ قبل از شروع ارزیابی بسازید. انتخاب برنده با داده‌های آخر دیتاست، آن داده‌ها را برای ارزیابی مستقل قابل استفاده نمی‌کند.

| تنظیم پیش‌فرض | مقدار و رفتار |
|---|---|
| مرحله‌ها | سیگنال لانگ/شورت → خروج لانگ/شورت → ریسک لانگ/شورت → RSI لانگ/شورت → scale لانگ/شورت → حساب مشترک |
| طول هر مرحله | ۱۰ cycle؛ قابل تغییر با `--stage-cycles` |
| snapshot | پایان هر دور ۱۱مرحله‌ای؛ پیش‌فرض هر ۱۱۰ cycle؛ در حالت جهت‌دار از طول دور محاسبه می‌شود |
| پایان اجرا | نامحدود تا `Ctrl+C`؛ `--auto-cycles` حد می‌گذارد و باید مضرب طول مرحله باشد |
| جست‌وجوی هر cycle | همان funnel توسعه، validation، stress و walk-forward موجود؛ هیچ‌کدام برای سرعت حذف نشده‌اند |
| آزمون بازه‌های snapshot | ۱۰ finalist روی ۵۰ بازهٔ یکسان؛ مجموع ۵۰۰ بک‌تست؛ قابل تنظیم با `--random-audit-tests` و `--random-audit-top` |

### هوشمندی و کارایی

| تغییر | دلیل | ثبت/محدودیت |
|---|---|---|
| ارزیابی رتبه‌بندی مدل روی بخش کنارگذاشته‌شدهٔ سابقهٔ جست‌وجو | کاهش اعتماد به مدل کم‌کیفیت | `validation_rank_correlation` در `surrogate_search.json`؛ این بخش reporting OOS نیست |
| سهم تطبیقی انتخاب | با ضعف مدل، تصادفی بیشتر؛ با کیفیت بهتر، تمرکز بیشتر روی پیش‌بینی | کیفیت ۲۵–۵۰٪، عدم‌قطعیت ۲۰٪، تنوع ۲۰٪، تصادفی ۱۰–۳۵٪؛ گردکردن و محدودیت تنوع روی تعداد نهایی اثر دارند |
| crossover بر اساس جهت | حفظ مجموعهٔ مرتبط دوره‌ها و تنظیمات یک والد | mutation و اعتبارسنجی بعد از ترکیب همچنان اجرا می‌شوند |
| حذف تنظیمات غیرفعالِ تکراری | پارامتر بی‌اثر باعث بک‌تست مجدد نشود | مثلاً تغییر آستانهٔ ATR هنگام خاموش بودن فیلتر آن جهت |
| انتقال برندگان بین مرحله‌ها فقط به‌عنوان والد | امتیاز قدیمی با baseline جدید، مشاهدهٔ معتبر برای آموزش مدل نیست | مدل پس از ارزیابی دوباره از نتیجه یاد می‌گیرد |
| cache اندیکاتورها و کراس‌های کم‌تعداد | محاسبهٔ مشترک فقط یک بار؛ تشخیص کراس به‌صورت برداری | کلید cache شامل فایل داده، نسخهٔ فایل، بازه و دورهٔ اندیکاتور است |
| گزارش خودکار آزمون بازه‌ها | نیاز به فرمان جداگانه برای گزارش از بین برود | از نتایج محاسبه‌شده استفاده می‌کند؛ هیچ بک‌تست اضافه برای اکسل ندارد |

این تغییرات تضمین رسیدن سریع‌تر به سود بیشتر نیستند؛ کیفیت نتیجه باید در آزمایش خود کمپین سنجیده شود. آزمون بازه‌های snapshot بخشی از انتخاب برنده است و ارزیابی مستقل روی دادهٔ ندیده محسوب نمی‌شود.

## خروجی و چارت‌ها

| فایل/شیت | محتوا |
|---|---|
| `best_params.json` | تنظیمات برنده، شامل overrideهای هر جهت |
| `auto_report.xlsx` / `snapshot_report.xlsx` | گزارش کلی در پایان هر مرحله، و نسخهٔ snapshot در پایان دور کامل |
| `random_window_report.xlsx` | گزارش خودکار بازه‌های برندهٔ snapshot، آمار، پارامترها و دنبالهٔ سود معاملات |
| `surrogate_search.json` | کیفیت پیش‌بینی، تعداد نمونه و سهم‌های انتخاب مدل |
| `Charts` | چارت‌های بزرگ با فاصلهٔ عمودی ثابت، legend پایین و برچسب‌های محور با تراکم محدود |
| جدول‌های Dashboard و سایر شیت‌ها | اعداد دقیق؛ برچسب‌های عددی فقط در چارت‌های تک‌سری و کم‌تعداد نمایش داده می‌شوند |

اصلاح چارت‌ها هنگام **تولید گزارش جدید** اعمال می‌شود؛ فایل‌های قدیمی روی دیسک تغییر داده نشده‌اند. این چیدمان در خروجی optimize، گزارش معاملات و evaluate_params مشترک است.
