# Optimize خودکار Pulse

به‌روزرسانی ۲۰۲۶-۰۹-۱۱: دستورهای جاری در [راهنمای سادهٔ اجرا](COMMANDS_FA.md) هستند.
پیش‌فرض Quality اکنون چهار چرخه با قیف `256 → 64 → 32 → WF 16×3 → 16` است؛
runner و `python optimize.py pulse` از یک تنظیم مشترک استفاده می‌کنند.
نام پوشهٔ خودکار runner با `quality_v3_` شروع می‌شود. اعداد 98 و مسیر Phase A
در ادامه، شرح نسخهٔ قدیمی‌اند و پیش‌فرض فعلی نیستند.

به‌روزرسانی ۲۰۲۶-۰۹-۱۰: پیش‌فرض runner اکنون Quality با ۴۶ متغیر مستقل
Long/Short و دو کلید ثابت برای فعال‌بودن هر دو جهت است؛ اهرم و حجم نیز مستقل‌اند.
راهنمای جاری: [Pulse جدید](PULSE_UPGRADES_FA.md).
توضیحات Phase A و بودجهٔ ۳۹۲ ترکیب در ادامه فقط با `--profile signal` صدق می‌کنند؛
تعدادهای قدیمی Full در این یادداشت تاریخی دیگر نمایندهٔ شبکهٔ فعلی نیستند.

از ریشهٔ پروژه اجرا کنید:

```powershell
python run_pulse_optimize.py --profile signal --performance normal
```

دیدن برنامه بدون اجرای بک‌تست: `python run_pulse_optimize.py --dry-run`.
حالت فشار سیستم: `power_saving`، `normal`، `boost`. محدودکردن پردازشگر: `--workers 4`. فایل دیگر: `--data-file PATH`؛ Pulse به کندل 1m نیاز دارد.

## بازه‌های وابسته به داده

ستون زمان فایل برای تشخیص پوشش خوانده می‌شود. آخرین کندل فایل فعلی `2026-07-31 23:59` است.

| مرحله | شروع شامل | پایان غیرشامل | کاربرد |
|---|---|---|---|
| Stress | 2025-02-01 | 2025-08-01 | رژیم قدیمی‌تر |
| Validation | 2025-08-01 | 2025-11-01 | ارزیابی منتخب‌ها |
| Discovery | 2025-11-01 | 2026-08-01 | جست‌وجوی پارامتر |
| Final | 2025-08-01 | 2026-08-01 | رتبه‌بندی در ۱۲ ماه اخیر |

Walk-forward سه بخش زمانی دارد. Final با مراحل دیگر هم‌پوشانی دارد و دادهٔ کاملاً دیده‌نشده نیست. ACCEPT ابزار غربال پژوهشی است، نه تضمین سود یا مجوز اجرای واقعی.

`pulse_strategy_config.py::AUTO_DATE_DEFAULTS` توسعه را ۱۲ ماه، Stress را شش ماه و Validation را سه ماه تعیین می‌کند. با افزایش داده، مرزها نسبت به انتهای جدید جابه‌جا می‌شوند. ماه اول فایل فعلی عمداً خارج از پنجرهٔ اخیر است. Stress در صورت نیاز به ابتدای فایل محدود می‌شود؛ دادهٔ ناکافی برای توسعه و Stress خطای واضح می‌دهد. پیش‌فرض MA تغییر نکرده است. گزینه‌های صریح `--rolling-...` اولویت دارند؛ `--date-policy fixed` تاریخ دستی را حفظ می‌کند.

## پارامترها و بودجه

| دسته | تعداد | محل / توضیح |
|---|---:|---|
| همهٔ فیلدهای تنظیمات | 26 | `PulseConfig`؛ شامل سرمایه، هزینه و کلیدهای اجرایی |
| پروفایل full | 7 | سه پارامتر سیگنال و چهار پارامتر اندازه/ریسک؛ 42,336 ترکیب اسمی |
| Phase A، پروفایل signal | 3 | `PHASE_A_GRID`، مشترک Long و Short |
| Phase B، پروفایل focused یا directional | 6 | سه پارامتر برای هر جهت؛ در شبکهٔ اولیه 153,664 ترکیب |

| پارامتر Phase A | مقادیر |
|---|---|
| `breakout_lookback_bars` | 15, 20, 30, 45, 60, 90, 120 |
| `stop_atr_mult` | 1.25, 1.5, 1.75, 2, 2.25, 2.5, 3 |
| `max_hold_bars` | 10, 15, 20, 30, 45, 60, 90, 120 |

فضای Phase A برابر 7×7×8 = **392 ترکیب** است. پیشنهاد شروع: سقف **4 سیکل × 98 نامزد جدید**؛ اتمام فضای معتبر می‌تواند اجرا را زودتر متوقف کند. روی همین شبکه، 100 سیکل یا 1000 ترکیب یکتا اطلاعات جدیدی نمی‌سازد. آزمون مولد با seed پیش‌فرض پوشش هر 392 ترکیب را تأیید می‌کند؛ بک‌تست واقعی این 392 ترکیب در زمان آماده‌سازی اسکریپت اجرا نشده است.

قیف هر سیکل: 98 Discovery → حداکثر 32 Validation → 16 Stress → 8 نامزد × 3 بخش Walk-forward → 8 Final. ارزیابی‌های اضافی در عدد 392 حساب نشده‌اند. حذف زودهنگام روی زیر‌بازهٔ Discovery در این اسکریپت خاموش است. پس از چهار سیکل، snapshot هشت نتیجهٔ برتر ساخته می‌شود. تعداد سیکل به‌تنهایی کیفیت یا سودآوری را تعیین نمی‌کند.

## محل ذخیره و ادامه

پوشهٔ اجرا هنگام شروع چاپ می‌شود: `outputs/pulse/optimize/phase_a_<data_end>_<revision>/`.

| فایل / مسیر | محتوا |
|---|---|
| `pulse_strategy_config.py` | پیش‌فرض‌ها و `FULL_PARAM_GRID` |
| `param_grids/pulse_focused.json` | شبکهٔ مستقل پروفایل focused |
| `<campaign>/best_params.json` | پارامترهای برنده |
| `<campaign>/auto_state.json` | checkpoint و تنظیمات اجرا |
| `<campaign>/cycles/` | نتایج مراحل و سیکل‌ها |
| `<campaign>/snapshots/cycles_000004/` | snapshot و گزارش‌های مشترک optimizer |

همان دستور روی همان نسخهٔ فایل، اجرای موجود را خودکار ادامه می‌دهد؛ سقف مجموع سیکل‌ها همچنان چهار است. تغییر مسیر، حجم یا زمان ویرایش فایل داده، revision و پوشهٔ تازه می‌سازد؛ اصلاح داده با انتهای ثابت نیز با نتایج قدیمی مخلوط نمی‌شود. تغییر صرفاً زمان ویرایش هم اجرای تازه می‌سازد.

اسکریپت از تنظیمات پیش‌فرض Phase A شروع می‌کند و برندهٔ جهت‌دار قبلی را وارد اجرای تازه نمی‌کند. در استفادهٔ مستقیم از `optimize.py`، پوشهٔ قدیمی تاریخ‌های ثبت‌شده را حفظ می‌کند؛ برای دادهٔ جدید پوشهٔ جدید بدهید. برندهٔ optimizer خودکار جایگزین پیش‌فرض‌های کد نمی‌شود.

## انتقال از Full به Focus

دستور ۱۰ سیکل با `--profile full --auto-tests 39 --auto-cycles 10` تا ۳۹۰ نامزد از فضای بزرگ‌تر 42,336 ترکیبی را بررسی می‌کند؛ دیگر پوشش تقریباً کامل Full نیست. اسکریپت `run_pulse_optimize.py` همچنان پروفایل سه‌پارامتری signal را اجرا می‌کند. برای Full، دستور مستقیم `optimize.py --profile full` را استفاده کنید.

پس از پایان این اجرا، برای مرحلهٔ بعد از `--profile focused --base-source best --base-params outputs/pulse/optimize/full_10cycles/best_params.json` و یک `--output-dir` جدید مثل `outputs/pulse/optimize/focused_after_full` استفاده کنید. سرمایه، هزینه و ریسک از فایل برنده حفظ می‌شوند و فقط شش پارامتر جهت‌دار تغییر می‌کنند. محدودهٔ اولیهٔ Focus اکنون به‌صورت مستقل آماده است؛ محدودکردن آن به همسایگی برنده را پس از بررسی نتایج انجام دهید. در حال حاضر هیچ محدوده‌ای به عنوان برنده فرض نشده است.

سرمایه، هزینه‌ها، محدودیت سفارش و کلید اجرایی `optimize` فقط در config یا فایل پارامتر پایه هستند و داخل Grid پذیرفته نمی‌شوند. ATR20 ثابت و بخشی از تعریف سیگنال فعلی است. overrideهای جهت در config با `None` از سیگنال مشترک ارث می‌برند؛ Focus آن‌ها را مستقل جست‌وجو می‌کند. اگر از Full قبلی checkpoint ساخته‌اید، به‌دلیل تغییر شبکه و پشتیبانی اهرم پوشهٔ جدید انتخاب کنید.

| پارامتر اندازه در Full | مقادیر | معنا |
|---|---|---|
| `risk_per_trade` | 0.001, 0.0025, 0.005 | سهم equity برای زیان اسمی تا استاپ |
| `max_gross_exposure` | 1, 2, 3 | سقف ارزش موقعیت تقسیم بر equity |
| `leverage` | 1, 2, 3 | اهرم واقعی موقعیت در موتور مشترک |
| `trade_amount_percent` | 0.25, 0.5, 0.75, 1 | سقف سهم equity برای مارجین، نه اندازهٔ اجباری معامله |

محدودیت ریسک و exposure ممکن است باعث شوند اهرم بالاتر اندازه را افزایش ندهد. حجم نهایی کمترین مقدار مجاز میان سقف ریسک، exposure، مارجین و نقدینگی با ذخیرهٔ کارمزد است. هزینه‌ها به ارزش موقعیت اعمال می‌شوند. استاپ دورتر از آستانهٔ liquidation برای ورود اهرمی رد می‌شود؛ عبور گپی از آستانه در Open با مسیر liquidation مشترک موتور پردازش می‌شود. این مدل، liquidation سادهٔ موتور است و تطبیق قوانین یک صرافی مشخص نیست.
