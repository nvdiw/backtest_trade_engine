# Pulse: کیفیت ورود، خروج و حجم معامله

از ۲۰۲۶-۰۹-۱۰، پروفایل پیش‌فرض `quality` در هر سه مسیر
`python optimize.py pulse`، `run_pulse_optimize.py` و `run_pulse_200.py`
۴۶ متغیر مستقل (۲۳ متغیر برای هر جهت) را در اختیار جست‌وجو می‌گذارد.
`enable_long` و `enable_short` هر دو روی `[true]` ثابت‌اند؛ هر نامزد ترکیب
دو جهت را در یک حساب مشترک ارزیابی می‌کند. قبلاً Quality اهرم و حجم را نداشت،
و `run_pulse_optimize.py` فقط سه متغیر پروفایل `signal` را جست‌وجو می‌کرد.

```powershell
python run_pulse_optimize.py --dry-run
python run_pulse_optimize.py --workers 4
```

دستور دوم چهار cycle با ۹۸ نامزد در هر cycle دارد. برای بودجهٔ قابل‌تغییر:

```powershell
python run_pulse_200.py --cycles 4 --tests 128 -w 4
```

برای Phase A سه‌متغیرهٔ قدیمی، `--profile signal` را به
`run_pulse_optimize.py` بدهید. `--profile sizing` فقط اندازه و ریسک را جست‌وجو می‌کند.
Resume شبکهٔ ذخیره‌شدهٔ کمپین قبلی را نگه می‌دارد؛ برای آزمایش متغیرهای جدید،
کمپین تازه لازم است. نام خودکار پوشه‌های runnerها اکنون `v2` دارد.
فایل‌ها و پارامترهای کمپین فعال قبلی جایگزین نشده‌اند.

## قابلیت‌های قابل‌بهینه‌سازی

دو قابلیت جدیدتر:

- `max_atr_expansion`: نسبت ATR فعلی به میانگین ۶۰ ATR قبلی را محدود می‌کند؛
  `[0, 1.5, 2, 3]`، صفر خاموش. ورود هنگام جهش نوسان رد می‌شود. به ۸۱ کندل
  پیوسته نیاز دارد و پس از شکاف داده دوباره گرم می‌شود.
- `stagnation_exit_bars`: اگر بعد از N کندل، بهترین پیشرفت قیمت بسته‌شدن از
  ابتدای معامله کمتر از `stagnation_min_progress_r` برابر ریسک قیمتی اولیه باشد،
  در بازشدن کندل بعد خارج می‌شود. N برابر `[0, 10, 30, 60]` و حد پیشرفت
  `[0.25, 0.5, 1]` است. صفر برای N خاموش است. این خروج مخصوص معامله‌ای است که
  هنوز پیشرفت کافی نداشته؛ trailing مسئول حفظ سود پس از حرکت مطلوب است.

هر سه پارامتر برای Long/Short مستقل‌اند و مقدار پایهٔ دو قابلیت خاموش است.
مقایسهٔ توسعه‌ای در `outputs/pulse/decision_guards_evaluation_20260910` نشان داد
اثرشان وابسته به بازه است: محدودکنندهٔ نوسان در دو بازه زیان را کمتر و در یک
بازه بیشتر کرد؛ خروج بدون پیشرفت نیز در یک بازه بهتر و در دو بازه بدتر بود.
ترکیب ثابت آن‌ها برتری عمومی نشان نداد؛ بنابراین به‌طور اجباری فعال نشده‌اند.

گلوگاه گزارش Excel نیز اصلاح شد: ابعاد شیت بیرون حلقهٔ ردیف‌ها محاسبه می‌شوند،
اندازهٔ ستون‌ها فقط از نمونهٔ محدود خوانده می‌شود و workbook تنها یک بار ذخیره
می‌شود. `KeyboardInterrupt` در حلقهٔ قبلی یعنی قطع پردازش قبل از رسیدن به چارت.

نام‌های جدول پسوند پارامتر هستند. در Quality هرکدام دو نسخهٔ مستقل با
پیشوند `long_` و `short_` دارند؛ حتی اهرم، سهم مارجین، ریسک و exposure.
مثلاً `long_entry_score_min` و `short_entry_score_min` جدا انتخاب می‌شوند.
پارامترهای بدون پیشوند برای سازگاری با فایل‌های قدیمی مقدار پایه‌اند و
وقتی override یک جهت مشخص باشد، مقدار مختص همان جهت اولویت دارد.

| متغیر | معنا | مقادیر Quality |
|---|---|---|
| `entry_score_min` | حداقل امتیاز ورود از ۱۰۰؛ صفر خاموش | 0, 40, 55, 70 |
| `failed_breakout_bars` | بازگشت قیمت بسته‌شدن به داخل کانالِ ثبت‌شده هنگام سیگنال، در اولین N کندل معامله، خروج در بازشدن کندل بعد | 0, 3, 6, 12 |
| `breakeven_activation_r` | پس از سود قیمتی معادل N برابر فاصلهٔ حدضرر اولیه، انتقال stop برای پوشش هزینه‌های مدل | 0, 1, 1.5, 2 |
| `leverage` | اهرم واقعی حساب مارجین | 1, 2, 3, 5, 8, 10 |
| `trade_amount_percent` | سقف سهم سرمایه برای مارجین؛ 0.05 یعنی ۵٪ | 0.05, 0.1, 0.25, 0.5, 1 |
| `risk_per_trade` | بودجهٔ زیان تا stop، بدون هزینه و gap | 0.001, 0.0025, 0.005 |
| `max_gross_exposure` | سقف ارزش اسمی موقعیت نسبت به equity | 1, 2, 3, 5 |

امتیاز ورود چهار جزء هم‌وزن دارد: محل close در دامنهٔ کندل در جهت معامله،
فاصلهٔ شکست نسبت به ATR، efficiency بیست‌ کندل و حجم نسبت به میانگین بیست
کندل قبلی. هر جزء بین صفر و یک محدود می‌شود و حداکثر ۲۵ امتیاز دارد؛
نسبت حجم ۲ و فاصلهٔ شکست یک ATR سقف مؤلفه‌های مربوطه‌اند.
این امتیاز احتمال آماری موفقیت نیست. فیلترهای صریح قبلی همچنان برقرارند.
پارامترهای جدید برای Long و Short نیز override مستقل دارند، مانند
`long_entry_score_min`؛ پروفایل `directional` آن‌ها را می‌بیند.

سیگنال‌های ورود و خروج بر کندل بسته‌شده تکیه دارند. stop جدید از کندل بعد
مؤثر است و هرگز شل‌تر نمی‌شود. stop و gap بر خروج زمان‌بندی‌شده اولویت دارند.
قیمت سربه‌سر کارمزد ورود/خروج و slippage خروج را پوشش می‌دهد؛ funding و
gap نامطلوب همچنان می‌توانند زیان بسازند.

حجم، کمینهٔ چهار محدودیت است: بودجهٔ ریسک/فاصلهٔ stop، سقف exposure،
مارجین تخصیص‌یافته × اهرم، و وجه نقد کافی برای مارجین و کارمزد.
پس دو اهرم متفاوت ممکن است تعداد واحدهای معاملهٔ برابر بسازند؛ این به معنی
تست‌نشدن اهرم نیست. سرمایه و هزینه‌های اجرا وارد grid نشده‌اند.

## فعالیت مداوم و نتیجهٔ آزمایش

هدف ماهانهٔ ۸٪ صرفاً گزارش است؛ نه مانع ورود است و نه معامله‌ای را می‌بندد.
`max_hold_bars` مدت یک معامله را محدود می‌کند، نه عمر اجرای ربات را.
این پروژه بک‌تست است؛ آزمون‌ها آمادگی یک سرویس معاملهٔ زندهٔ دائمی را اثبات نمی‌کنند.

سه قابلیت جدید در تنظیمات پایه خاموش‌اند و Optimize می‌تواند آن‌ها را روشن کند.
آزمایش توسعه‌ای ۸ ترکیب در سه پنجرهٔ سه‌ماهه، با پارامترهای کمپین موجود،
برتری سودِ ترکیب پیشنهادی را تأیید نکرد؛ بنابراین پارامتر جدیدی به‌عنوان برنده
روی فایل قبلی اعمال نشده است. خروجی قابل‌بازتولید در
`outputs/pulse/upgrade_evaluation_20260910/comparison.csv` و تنظیمات دقیق در
`experiment.json` همان پوشه هستند. دستور بازتولید با پوشه‌ای تازه:

```powershell
python evaluate_pulse_upgrades.py --params-file PATH_TO_PARAMS.json --output-dir outputs/pulse/another_evaluation
```

این پنجره‌ها ممکن است در انتخاب فایل پایه استفاده شده باشند؛ نتایج، holdout
مستقل نیستند. برای سنجش حساسیت به هزینه‌ها، سناریوهای موجود adverse/severe
و برای ارزیابی مستقل، مسیر پژوهشی walk-forward/holdout پروژه را استفاده کنید.

## مبنای پژوهش

پژوهش [AQR دربارهٔ دنبال‌کردن روند](https://www.aqr.com/-/media/AQR/Documents/Insights/Journal-Article/AQR-JPM-Fall-2017.pdf)
روند، رژیم‌های مختلف بازار و هزینه‌ها را بررسی می‌کند؛ این شاهد مستقیم برای
سوددهی Pulse روی بیت‌کوین یک‌دقیقه‌ای نیست. طراحی سه قابلیت فوق فرضیهٔ اجرایی
این پروژه است. [پژوهش بیش‌برازش بک‌تست](https://www.davidhbailey.com/dhbpapers/backtest-prob.pdf)
نیز دلیل نگه‌داشتن حالت خاموش، آزمایش جزءبه‌جزء و جداکردن دادهٔ انتخاب از
ارزیابی مستقل را توضیح می‌دهد.
