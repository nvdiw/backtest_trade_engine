# ساختار ساده استراتژی‌ها

دو نام اصلی پروژه **MA** و **Pulse** هستند. Pulse اکنون استراتژی یک‌دقیقه‌ای Rolling Range Breakout است و از موتور، CSV logger و chart renderer مشترک استفاده می‌کند. قوانین، پارامترها و فرمان‌های آن در [راهنمای Pulse](PULSE_GUIDE_FA.md) آمده‌اند.

| بخش | MA | Pulse |
|---|---|---|
| ماژول مستقل | `ma_strategy.py` | `pulse_strategy.py` |
| انتخاب در optimizer | `--strategy ma` | `--strategy pulse` |
| داده پیش‌فرض | `data_candle/btc_15m_data_2018_to_2026.csv` | `data_candle/btc_1m_data_2025_to_2026.csv` |
| بک‌تست مستقیم | `outputs/ma/backtest/` | `outputs/pulse/backtest/` |
| optimize و Auto | `outputs/ma/optimize/` | `outputs/pulse/optimize/` |
| ارزیابی پارامتر ثابت | `outputs/ma/evaluate/` | `outputs/pulse/evaluate/` |
| walk-forward پژوهشی | `outputs/ma/research/` | `outputs/pulse/research/` |
| holdout نهایی | `outputs/ma/holdout/` | `outputs/pulse/holdout/` |

CSV، XLSX، نمودارها، بهترین پارامترها و checkpointهای هر اجرا زیر پوشه همان اجرا قرار می‌گیرند. ساختار داخلی گزارش‌ها تغییر نکرده است. کدها در ریشه پروژه باقی می‌مانند؛ موتور و ابزارهای مشترک کپی نمی‌شوند.

## پیش‌فرض یکسان بک‌تست مستقیم

| رفتار اجرای `python ma_strategy.py` یا `python pulse_strategy.py` | پیش‌فرض |
|---|---|
| فایل ورودی MA | فایل ۱۵دقیقه‌ای؛ تشخیص فاصله از زمان کندل |
| فایل ورودی Pulse | فایل یک‌دقیقه‌ای؛ فاصلهٔ دیگر رد می‌شود |
| CSV معاملات، خلاصه، XLSX و CSV ماهانه | ساخته می‌شوند؛ بازه بدون معامله هم خروجی معتبر دارد |
| `params.json` و `result.json` | ساخته می‌شوند |
| `chart.png` | ساخته می‌شود |
| پنجره نمودار تعاملی | نمایش داده می‌شود؛ فایل‌ها پیش از نمایش نوشته شده‌اند |
| بازشدن خودکار Excel یا CSV | انجام نمی‌شود؛ فایل‌ها را می‌توان دستی باز کرد |

گزینه‌های مشترک: `--no-show-chart` فقط نمایش پنجره را خاموش می‌کند و PNG را نگه می‌دارد؛ `--no-chart` نمایش و PNG خودکار را خاموش می‌کند؛ `--save-chart PATH` مسیر تصویر صریح را تعیین می‌کند و حتی با `--no-chart` ذخیره می‌کند؛ `--no-excel` فقط XLSX را خاموش می‌کند. این رفتار برای بک‌تست مستقیم است؛ optimizer پنجره و گزارش مستقل هر کاندیدا را غیرفعال نگه می‌دارد و گزارش تجمیعی خودش را می‌سازد.

## قواعد جداسازی

- CLIها پوشه پیش‌فرض را از هویت استراتژی انتخاب می‌کنند. `--output-dir` همچنان مسیر دلخواه را می‌پذیرد؛ برای آزمایش‌های متعدد یک استراتژی، مثلاً `outputs/ma/optimize/focus_02` انتخاب کنید.
- فایل `strategy_workspace.json` مالک و نوع خروجی را ثبت می‌کند. استفاده استراتژی دیگر از آن پوشه یا خواندن پارامترهای داخل workspace آن متوقف می‌شود. پوشه ارزیابی و optimize نیز نباید یکسان باشد.
- قفل سیستم‌عامل روی `.run.lock` از دو نویسنده هم‌زمان در یک پوشه جلوگیری می‌کند. پس از خروج یا crash قفل آزاد می‌شود؛ باقی‌ماندن خود فایل نشانه اجرای فعال نیست. اجراهای جدا در ترمینال‌های جدا و پوشه‌های جدا قابل انجام‌اند.
- best پیش‌فرض هر استراتژی از `outputs/<name>/optimize/best_params.json` خوانده می‌شود. اگر کمپین را در زیرپوشه دلخواه اجرا کردید، `--base-params` یا `--best-params` همان کمپین را صریح بدهید.
- مسیر فایل داده و performance هر اجرای CLI به workerهای همان پردازش منتقل می‌شود. هم‌زمانی CLIها در ترمینال‌های جدا پشتیبانی می‌شود؛ اجرای چند CLI در threadهای یک پردازش، به‌علت تنظیمات محیطی و کش‌های مشترک، قرارداد پشتیبانی‌شده نیست.

## خروجی‌های قبلی

فایل‌ها و کمپین‌های قبلی منتقل یا حذف نشده‌اند. برای ادامه‌شان همان مسیر قدیمی `--output-dir` را بدهید. از این پس اجرای بدون مسیر صریح از پوشه‌های جدید استفاده می‌کند. پوشه‌های قدیمی دارای `research_manifest.json` هنگام ثبت مالک با هویت استراتژی تطبیق داده می‌شوند؛ فایل پارامتر قدیمی بدون شناسنامه ممکن است قابل تشخیص نباشد، پس منبع آن را خودتان درست انتخاب کنید. کنترل سازگاری checkpointهای موجود همچنان برقرار است.

مثال اجرای MA در مسیر جدید:

```powershell
python optimize.py --strategy ma --auto --profile focused --performance normal --auto-cycles 100 --output-dir outputs/ma/optimize/focus_01
```

## قرارداد نگهداری استراتژی‌ها

1. قوانین Pulse در `pulse_strategy.py` نگهداری شوند؛ تغییر MA برای اضافه‌کردن منطق Pulse لازم نیست.
2. تنظیمات، gridها، validator، warmup و کش اندیکاتورها متعلق به ماژول Pulse باشند. از `strategy_config.py` مربوط به MA برای تنظیمات Pulse استفاده نشود.
3. قرارداد `StrategyAdapter` و خروجی معیارهای optimizer رعایت شود. `TradeEngine`، اندیکاتورها و گزارش‌سازها ابزار مشترک باقی بمانند.
4. خروجی مستقیم با `output_path(identifier, 'backtest')` تعیین شود. توابع نویسنده از `@output_session` و `claim_output` استفاده کنند؛ اجرای optimize داخل worker نباید گزارش مستقل بنویسد.
5. Pulse اکنون `STRATEGY_READY=True` دارد. برای پلاگین آینده که هنوز قوانین ندارد، مقدار False مانع اجرای تصادفی می‌شود.
6. تست‌های استراتژی جدید جدا باشند؛ تست‌های مشترک و MA نیز بعد از تغییر موتور اجرا شوند. جداسازی فایل‌ها جای تست سازگاری تغییرات موتور مشترک را نمی‌گیرد.

افزودن پلاگین جدید با `module:function` نیازمند تغییر optimizer نیست؛ alias کوتاه اختیاری است. ماژول MA نیز فقط هنگام انتخاب آن بارگذاری می‌شود. ابزارهای اصلی مشترک همچنان وابستگی لازم همه استراتژی‌ها هستند.
