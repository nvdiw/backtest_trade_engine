# Pulse — Rolling Range Breakout 1m

Pulse یک فرضیه پژوهشی برای شکست محدوده است؛ پیاده‌سازی و عبور از تست‌ها به معنی سودآوری نیست. استراتژی اصلی MA جایگزین نشده است.

## معماری و خروجی‌ها

| مسئولیت | کد مشترک / ماژول |
|---|---|
| قوانین، وضعیت مسلح‌بودن، پارامترها و کش مستقل | `pulse_strategy.py` |
| کشف پلاگین، تنظیمات و warmup، اجرای کاندیدا | `strategy_adapter.py`؛ انتخاب `--strategy pulse` |
| پرشدن سفارش، لغزش، کارمزد، حساب، اندازه موقعیت و مرجع استاپ | `trade_engine.py` |
| ثبت معاملات و workbook | `trade_csv_logger.py` و `excel_charts.py` |
| نمودار تعاملی و تصویر | `chart_renderer.py` از طریق `TradeEngine.render_strategy_chart` |
| گزارش ماهانه معاملات بسته‌شده | `check_monthly_data.py` از طریق موتور |
| grid، smart، Auto، checkpoint، walk-forward و XLSX | `optimize.py` |
| ارزیابی پارامتر ثابت در پنجره‌های تقویمی | `evaluate_params.py` |

برای ساخت این ماژول، قرارداد Adapter، ساختار MA و استراتژی نمونه، روش‌های عمومی موتور و تست‌های اجرای کندل/optimizer بررسی شدند. Adapter در این پروژه یک تابع بک‌تست را صدا می‌زند؛ خود استراتژی ترتیب رویدادهای کندل را کنترل می‌کند و موتور عملیات مالی را انجام می‌دهد. Pulse همین قرارداد را رعایت می‌کند.

پیش‌فرض نمایش و ذخیره اکنون با MA یکسان است: بک‌تست مستقیم هم `chart.png` می‌سازد و هم پنجره نمودار را نمایش می‌دهد. برای اجرای بدون پنجره و با حفظ همه فایل‌ها `--no-show-chart` بدهید. CSV/XLSX خودکار باز نمی‌شوند. JSON و گزارش ماهانه نیز قبل از نمایش نمودار آماده می‌شوند.

لاگ زندهٔ ترمینال نیز پیش‌فرض فعال است: ورود/خروج، قیمت، زمان، موجودی قبل و بعد، PnL، کارمزد، سود، مدت معامله و خلاصهٔ `BACKTEST FINISHED` از `TradeEngine` چاپ می‌شوند. `--quiet` فقط چاپ ترمینال را خاموش می‌کند؛ فایل‌ها و نمودار مستقل‌اند. workerهای optimize حتی با درخواست verbose ساکت می‌مانند. Pulse قانون cooldown مربوط به MA ندارد و پیام فعال‌شدن cooldown صفر چاپ نمی‌کند.

خروجی مستقیم پیش‌فرض:

```text
outputs/pulse/backtest/
  params.json
  result.json
  chart.png
  strategy_workspace.json
  trades/
    data_orders.csv
    data_orders.xlsx
    data_orders_summary.csv
  monthly/
    monthly_data_orders.csv
```

optimize در `outputs/pulse/optimize/` و ارزیابی ثابت در `outputs/pulse/evaluate/` قرار می‌گیرند. مسیر صریح کمپین‌ها همچنان قابل انتخاب است. در workerهای optimize فایل معامله و نمودار تولید نمی‌شود؛ گزارش تجمیعی optimizer تولید می‌شود. نمودار Pulse همان zoom، drag و توضیح روی علامت معامله را دارد؛ خطوط MA/RSI با اندیکاتورهای نامرتبط پر نشده‌اند، بلکه renderer مشترک اکنون overlay و پنل اندیکاتور قابل‌نام‌گذاری می‌پذیرد.

## قواعد دقیق

- `upper[t]` بیشترین High در N کندل **قبل** از t است؛ `lower[t]` کمترین Low در N کندل قبل از t. High/Low خود t وارد کانال نمی‌شود. شکست با Close و نامساوی سخت محاسبه می‌شود.
- ATR برابر میانگین ساده ۲۰ True Range است؛ Wilder نیست. حداقل warmup برابر `max(N_long,N_short,20)+1` کندل پیوسته است. warmup قبل از شروع بازه، فقط اندیکاتورها را آماده می‌کند و معامله‌ای تولید نمی‌کند.
- سیگنال پس از بسته‌شدن کندل و ورود در Open کندل پیوسته بعدی با لغزش نامساعد است. روی آخرین کندل، روی کندل ناقص یا قبل از گپ، ورود مصنوعی ساخته نمی‌شود.
- استاپ از **قیمت واقعی ورود** و ATR کندل سیگنال ساخته می‌شود. استاپ روی همان کندل ورود هم فعال است. لانگ در گپ قیمتی با Open پایین‌تر و شورت با Open بالاتر اجرا می‌شود، سپس هزینه موتور اعمال می‌شود.
- کندل ورود، holding bar شماره ۱ است. پس از H کندل کامل، خروج در Open بعدی انجام می‌شود. استاپ در کندل H و نیز شکست استاپ در Open خروج، بر خروج زمانی اولویت دارد.
- یک موقعیت، بدون pyramiding، TP، trailing، limit entry، فیلتر حجم یا میانگین/RSI/ADX. سیگنال مخالف موقعیت را نمی‌بندد و reverse در همان قیمت اجرا نمی‌شود. بعد از خروج، تصمیم جدید فقط پس از یک Close معتبر و با سمت مسلح می‌تواند برای Open بعدی ساخته شود.
- بعد از ورود موفق، همان سمت غیرمسلح می‌شود. لانگ فقط با `close <= upper` و شورت فقط با `close >= lower` دوباره مسلح می‌شود؛ این بررسی هنگام بازبودن موقعیت هم انجام می‌شود.
- در گپ زمانی، warmup و arming از نو شروع می‌شوند. موقعیت باز با `min(stop,open)` برای لانگ و `max(stop,open)` برای شورت بسته و `data_gap_exit` ثبت می‌شود. OHLC نامعتبر رد می‌شود؛ سیگنال دوطرفه نامعتبر معامله تولید نمی‌کند و diagnostic دارد.
- ورودی باید یک‌دقیقه‌ای باشد. زمان‌ها مرتب، قیمت‌ها مثبت و متناهی و هندسه OHLC معتبر باشند. قالب Close time این پروژه شامل آخرین میلی‌ثانیه کندل است؛ مثلاً `00:00:59.999`. داده ناقص فعلی از محاسبات نهایی کنار گذاشته می‌شود.

## schema پارامترها

کلیدهای زیر، schema کامل تنظیمات `PulseConfig` هستند. مقدار `null` برای override سمت، یعنی ارث‌بری از مقدار مشترک.

| کلید | مقدار شروع | واحد / نقش |
|---|---|---|
| `breakout_lookback_bars` | 30 | تعداد کندل کانال |
| `stop_atr_mult` | 2.0 | ضریب ATR |
| `max_hold_bars` | 30 | تعداد کندل کامل نگهداری |
| `long_breakout_lookback_bars` | null | override کانال لانگ |
| `short_breakout_lookback_bars` | null | override کانال شورت |
| `long_stop_atr_mult` | null | override استاپ لانگ |
| `short_stop_atr_mult` | null | override استاپ شورت |
| `long_max_hold_bars` | null | override مدت لانگ |
| `short_max_hold_bars` | null | override مدت شورت |
| `atr_period` | 20 | ثابت؛ مقدار دیگر رد می‌شود |
| `enable_long` / `enable_short` | true / true | فعال‌بودن سمت‌ها |
| `balance` | 1000.0 | سرمایه شروع |
| `risk_per_trade` | 0.0025 | ریسک اسمی فاصله استاپ، بدون هزینه و گپ |
| `max_gross_exposure` | 1.0 | سقف ارزش ناخالص نسبت به equity؛ حداکثر 1 |
| `quantity_step` | 0.0 | صفر: گردکردن غیرفعال؛ مقدار مثبت: گردکردن رو به پایین |
| `min_quantity` | 0.0 | حداقل مقدار سفارش |
| `min_notional` | 0.0 | حداقل ارزش سفارش |
| `fee_rate` | 0.0005 | فرض پژوهشی نرخ کارمزد هر طرف |
| `slippage_rate` | 0.0001 | فرض پژوهشی لغزش نامساعد هر طرف |
| `funding_rate_per_8h` | 0.0 | funding تاریخی در داده موجود نیست؛ پیش‌فرض حذف‌شده |
| `optimize` | false | حالت داخلی worker؛ CLI بک‌تست همیشه گزارش‌دار است |

اندازه موقعیت از حداقل ریسک تقسیم بر فاصله استاپ، سقف exposure و نقدینگی قابل‌استفاده محاسبه می‌شود. برای کارمزد رفت‌وبرگشت تخمینی در سقف نقدینگی جا نگه داشته می‌شود. حداقل سفارش، مجوز افزایش اندازه و عبور از ریسک نیست؛ سفارش کوچک رد می‌شود. واحد پول باید با Quote داده سازگار باشد.

### Phase A: دقیقاً ۳۹۲ ترکیب

| پارامتر | مقادیر |
|---|---|
| `breakout_lookback_bars` | 15, 20, 30, 45, 60, 90, 120 |
| `stop_atr_mult` | 1.25, 1.50, 1.75, 2.00, 2.25, 2.50, 3.00 |
| `max_hold_bars` | 10, 15, 20, 30, 45, 60, 90, 120 |

پروفایل‌های `focused`، `full` و `signal` همین سه بعد مشترک را دارند. پروفایل `directional` شش override مستقل Phase B را جست‌وجو می‌کند و پیش‌فرض نیست. برای Phase A از config اولیه و overrideهای null شروع کنید؛ فایل برنده Phase B را به‌عنوان مبنای آزمایش مشترک استفاده نکنید. در Pulse تغییر پیوسته grid توسط Auto غیرفعال است؛ پس Auto پس از تمام‌شدن کاندیداهای تازه ممکن است زودتر از سقف سیکل متوقف شود. تکرار بی‌دلیل ۱۰۰ سیکل، ۳۹۲ ترکیب را به آزمایش مستقل بیشتری تبدیل نمی‌کند.

ریسک، ATR period و هزینه‌ها در grid قابل optimize نیستند. سناریوهای هزینه فقط برای stress استفاده می‌شوند. نرخ‌های پیش‌فرض ادعای نرخ واقعی هیچ صرافی نیستند.

## فرمان‌های واقعی پروژه

این دستورها در ریشه پروژه، هم در PowerShell و هم در CMD قابل اجرا هستند.

بک‌تست کوچک روی دو روز نخست داده محلی؛ CSV، XLSX، ماهانه، JSON و PNG به‌طور پیش‌فرض تولید می‌شوند:

```powershell
python pulse_strategy.py --start 2025-01-01 --end 2025-01-03 --output-dir outputs/pulse/backtest/demo
```

پنجره نمودار تعاملی با همان renderer پروژه:

```powershell
python pulse_strategy.py --start 2025-01-01 --end 2025-01-03 --show-chart --output-dir outputs/pulse/backtest/interactive
```

گزینه‌های `--no-chart`، `--save-chart PATH`، `--plot-max-candles 500`، `--no-excel`، `--trace`، `--params-file PATH` و `--set KEY=VALUE` نیز موجودند. برای نمونه، `--set short_max_hold_bars=45` فقط مدت شورت را تغییر می‌دهد. `--trace` رویدادهای تشخیصی را داخل result.json اضافه می‌کند و برای تست کوتاه مناسب است.

تست‌های Pulse و آزمون کل پروژه:

```powershell
python -m unittest test_pulse_strategy test_pulse_integration -v
python -m unittest discover -q
```

Smoke test optimizer، هشت کاندیدا با دو worker:

```powershell
python optimize.py --strategy pulse --mode smart --profile focused --tests 8 --date-policy fixed --start 2025-01-01 --end 2025-01-03 -w 2 --excel-top 2 --output-dir outputs/pulse/optimize/smoke
```

آزمایش کامل Phase A: جست‌وجوی ۳۹۲ ترکیب در سال ۲۰۲۵، انتخاب ثانویه در ژانویه تا مارس ۲۰۲۶. آوریل تا ژوئیه ۲۰۲۶ در این فرمان مصرف نمی‌شود. validation همچنان بخشی از انتخاب است، نه اثبات خارج از نمونه. آستانه‌های پایین پیشنهاد یک آزمایش قابل‌تکرار هستند و معیار سودآوری قطعی نیستند:

```powershell
python optimize.py --strategy pulse --mode grid --profile focused --base-source config --date-policy fixed --start 2025-01-01 --end 2026-01-01 --validation-start 2026-01-01 --validation-end 2026-04-01 --validation-top 20 --min-trades 100 --max-drawdown 25 --performance normal --output-dir outputs/pulse/optimize/phase_a_392
```

این فرمان با `--dry-run` بررسی شد و ۳۹۲ کاندیدا اعلام کرد؛ آزمایش کامل و holdout در این مرحله اجرا نشده‌اند. برای ادامه همان اجرای قابل‌ادامه، `--resume` را به همان فرمان اضافه کنید. از `--autopilot` یا `--directional` عمومی MA برای Pulse استفاده نکنید؛ انتخاب Phase B با `--profile directional` است.

## آزمون‌ها و محدودیت‌ها

`test_pulse_strategy.py` از داده مصنوعی برای کانال قبلی، ATR ساده، warmup، قیمت ورود بعدی، تغییر آینده، کندل ناقص، تقارن، استاپ کندل ورود، گپ قیمتی/زمانی، اولویت استاپ، زمان خروج، re-arm، منع reverse، هزینه دو سمت، گردکردن و حداقل سفارش، تشخیص داده نامعتبر و پایان داده استفاده می‌کند. `test_pulse_integration.py` جست‌وجوی چندپردازشی، schema و گزارش‌ها، دو سیکل Auto با resume، و منع optimize هزینه‌ها را بررسی می‌کند. تست‌های MA و renderer مشترک نیز اجرا می‌شوند.

نتیجه بررسی نسخه فعلی: **۱۵۶ تست کل پروژه پاس شد**، شامل ۲۱ تست Pulse. Smoke optimizer با هشت کاندیدا و دو worker بدون خطای اجرا تمام شد. ارزیابی ثابت یک پنجره ژانویه ۲۰۲۵ نیز گزارش XLSX تولید کرد، ولی نتیجه کیفی آن `FAIL` بود؛ این خطای اجرای نرم‌افزار نیست. این پنجره با بازه انتخاب اولیه هم‌پوشانی دارد و آزمون holdout نیست. نمودار PNG واقعی نیز از نظر نمایش خطوط، علامت معاملات و برچسب‌های زمان بازبینی شد.

در smoke واقعی ۲۰۲۵/۰۱/۰۱ تا قبل از ۲۰۲۵/۰۱/۰۳، تنظیمات شروع ۱۰۳ معامله بسته داشتند و زیان خالص حدود ۱۱۹٫۲۸ از سرمایه ۱۰۰۰ ثبت شد. این نتیجه شاهد سودآوری نیست و grid برای بهترکردن این عدد تغییر نکرده است.

محدودیت‌های مدل اجرا:

- timestamp دقیق برخورد داخل کندل از OHLC معلوم نیست؛ قیمت استاپ محافظه‌کارانه و زمان پایان همان کندل برای حسابداری استفاده می‌شود. خروج در Open زمان دقیق همان Open دارد.
- هزینه ورود و خروج طبق دفتر موجود موتور هنگام بسته‌شدن تسویه می‌شود. برای موقعیت باز، Pulse هزینه ورود و funding تخمینی انباشته را در equity و گزارش نهایی لحاظ می‌کند. خروج پایان بازه ساخته نمی‌شود؛ سود باز، باز باقی می‌ماند و کارمزد خروج انجام‌نشده اخذ نمی‌شود.
- funding موتور یک نرخ ثابت هزینه‌ای و تابع مدت نگهداری است؛ جریان تاریخی نرخ‌های مثبت/منفی یا تسویه واقعی صرافی نیست. با پیش‌فرض صفر، funding حذف شده و این موضوع در `funding_model` ثبت می‌شود؛ نتیجه برای perpetual با هزینه کامل تأییدشده نیست.
- spread، تأخیر، عمق، partial fill، tick size قیمت و برنامه تاریخی funding از OHLCV قابل بازسازی نیستند. لغزش ثابت فقط فرض اجرایی است. quantity step و minimumها قابل تنظیم‌اند اما از صرافی دریافت نمی‌شوند.
- Pulse با exposure حداکثر 1 و بدون اهرم پژوهش می‌شود. شبیه‌سازی نوع قرارداد و قوانین liquidation یک صرافی مشخص انجام نشده است. استاپ و ریسک اسمی، زیان گپ را محدود نمی‌کنند.
- هیچ اتصال سفارش، کلید API، paper trading یا live trading اضافه نشده است.
