# کمپین Pulse با تمرکز بر کیفیت معامله

اجرای ۲۰۰ cycle روی دادهٔ یک‌دقیقه‌ای موجود:

```powershell
python run_pulse_200.py
```

بررسی تنظیمات بدون شروع بک‌تست:

```powershell
python run_pulse_200.py --dry-run
```

انتخاب تعداد worker یا آزمایش کوتاه‌تر:

```powershell
python run_pulse_200.py -w 4
python run_pulse_200.py --cycles 2 --tests 32 -w 2
```

ادامه با نام یکتای پوشه یا مسیر آن:

```powershell
python run_pulse_200.py --resume quality_1m_v1_نام_نسخه --cycles 20
```

`--cycles` تعداد cycleهای **اضافی در همین اجرا** است. برای مثال بعد از ۵۰ cycle، برای رسیدن به مجموع ۲۰۰، مقدار ۱۵۰ بدهید. تنظیمات جست‌وجوی قبلی هنگام resume بازیابی می‌شوند. تعداد تست را برای یک کمپین جدید با `--tests` تعیین کنید؛ برای تغییر آن در کمپین موجود پوشهٔ جدید بسازید.

نسخهٔ ۱۵دقیقه‌ای:

```powershell
python run_pulse_200.py --timeframe 15m
```

فایل داده، پوشهٔ خروجی و grid پیش‌فرض هر timeframe جداست. `--data-file` و `--output-dir` نیز قابل تعیین هستند. نام خودکار پوشه از timeframe، نسخهٔ فایل داده و دستور جست‌وجو ساخته می‌شود. فاصلهٔ داده باید با timeframe اعلام‌شده سازگار باشد؛ فایل ۱m به‌صورت ضمنی به ۱۵m تبدیل نمی‌شود.

## چرا کمپین تازه؟

در خروجی محلی `outputs/pulse/optimize/full_10cycles` هنگام بررسی، ۵۰ cycle ثبت شده بود. بهترین عضو Hall of Fame وضعیت `REJECT`، بازده حدود منفی ۸۰٫۷٪، ۵۶۵۰ معامله و حدود ۶۹۲ واحد کارمزد داشت. grid آن فیلترهای کیفیت و trailing را شامل نمی‌شد. این مشاهده مربوط به همان داده و نسخهٔ استراتژی است.

کمپین تازه ۱۵ پارامتر را برای کانال شکست، stop، مدت نگهداری، فیلتر روند، efficiency، حجم نسبی، نسبت ATR به هزینهٔ رفت‌وبرگشت، اندازهٔ کندل سیگنال، gap ورود، trailing، cooldown و انتخاب long/short جست‌وجو می‌کند. پارامترهای زمانی بر حسب **تعداد کندل** هستند؛ gridهای ۱m و ۱۵m بازه‌های مستقل دارند و پارامتر برندهٔ یکی برای دیگری معتبر فرض نمی‌شود.

اهرم، ریسک هر معامله و هزینه‌ها در این دستور جست‌وجو نمی‌شوند. baseline کامل در checkpoint کمپین جدید ذخیره می‌شود؛ با defaults فعلی اهرم ۱، ریسک هر معامله ۰٫۲۵٪، کارمزد ۰٫۰۵٪ در هر سمت و slippage برابر ۰٫۰۱٪ است. اعداد نتیجه پس از هزینه‌های مدل هستند؛ funding تاریخی در این داده/دستور مدل نمی‌شود.

## استفاده از تاریخچه

حالت `--auto-learning-target profit-evidence` برای هر استراتژی در Auto قابل استفاده است و در اسکریپت جدید روشن است:

- برچسب یادگیری از ترکیب ۳۵٪ رتبهٔ نسبی و ۶۵٪ شواهد اقتصادی ساخته می‌شود؛ این وزن‌ها heuristic نسخهٔ اول‌اند، نه نتیجهٔ کالیبراسیون روی دادهٔ مستقل.
- شواهد اقتصادی سود خالص را نسبت به افت سرمایه و شدت ماه‌های منفی می‌سنجد؛ تعداد معاملات و نسبت ماه‌های مثبت اعتماد به نتیجهٔ مثبت را تعدیل می‌کنند. هزینه‌ها از سود دوباره کم نمی‌شوند.
- نتیجهٔ بدون معامله، liquidation و نتیجه‌ای که از قیود عبور نکرده، نمونهٔ ضعیف یادگیری محسوب می‌شود. خطای فنی worker به‌عنوان زیان استراتژی آموزش داده نمی‌شود.
- شواهد مرحله‌های Discovery، Validation، Stress، Walk-forward و Final وارد یادگیری می‌شوند؛ ضعیف‌ترین مرحلهٔ مشاهده‌شده نیز وزن دارد. تمام این‌ها دادهٔ انتخاب پارامتر هستند.
- پس از جمع شدن حداقل سه cycle با نمونه‌های کافی، اعتبارسنجی مدل پیشنهاددهنده روی cycleهای جدیدتر جست‌وجو انجام می‌شود. با تاریخچهٔ کمتر از تقسیم قبلی نمونه‌ها استفاده می‌شود. مدل ضعیف سهم بیشتری از جست‌وجو را به اکتشاف تصادفی اختصاص می‌دهد.
- حداکثر ۱۰هزار نمونهٔ نماینده برای مدل نگه داشته می‌شود؛ نتایج تفصیلی کمپین روی دیسک می‌مانند. تاریخچهٔ strategy یا timeframe دیگر خودکار مخلوط نمی‌شود.

حالت پیش‌فرض عمومی و کمپین‌های قدیمی همچنان `rank` است؛ resume منطق یادگیری ذخیره‌شده را حفظ می‌کند. انتخاب نهایی هنوز از رتبه‌بندی چندمرحله‌ای و وضعیت `ACCEPT/WATCH/REJECT` استفاده می‌کند؛ مدل پیشنهاددهنده مستقیماً پارامتر را بدون بک‌تست به برنده تبدیل نمی‌کند.

## تنظیمات و خواندن نتیجه

هر cycle حداکثر ۱۲۸ Discovery، ۲۴ Validation، ۱۲ Stress، هشت نامزد روی سه fold و چهار Final دارد: حداکثر **۱۹۲ بک‌تست در هر cycle**، یعنی ۳۸۴۰۰ ارزیابی برای ۲۰۰ cycle. نامزدهای حذف‌شده می‌توانند این تعداد را کم کنند. محدودیت معامله ۳۰ و افت سرمایه ۲۵٪ است؛ حداقل معاملات در foldها طبق منطق optimizer تعدیل می‌شود. زمان اجرا به طول داده، تعداد worker و سخت‌افزار وابسته است.

در cycleهای ۵۰، ۱۰۰، ۱۵۰ و ۲۰۰ snapshot ساخته می‌شود. `OVERVIEW.md` و sheet `Quick Compare` را برای بررسی نامزدها، `best_candidate_summary.json` را برای دلایل تصمیم و `best_params.json` را برای پارامتر رتبهٔ اول بخوانید. پارامتر checkpoint پیش از تشکیل Hall of Fame موقت است. امکان دارد تمام نامزدها رد شوند.

فیلتر جدید `max_entry_gap_atr` اگر قیمت open بعدی در جهت نامطلوب بیش از حد تعیین‌شده نسبت به close سیگنال فاصله گرفته باشد، ورود را رد می‌کند. مقدار صفر خاموش است؛ مقادیر اختصاصی long/short نیز پشتیبانی می‌شوند. تصمیم فقط با قیمت open همان لحظه و ATR کندل بسته‌شدهٔ قبلی گرفته می‌شود.

پشتیبانی واقعی ۱۵m شامل warmup، فاصلهٔ کندل، gap، زمان بسته‌شدن کندل و اجرای next-open است. بک‌تست ۱m همچنان بر دادهٔ یک‌دقیقه‌ای اجرا می‌شود.

بیشتر کردن تعداد تست‌ها به‌تنهایی اثبات افزایش سود نیست؛ انتخاب مکرر روی یک داده می‌تواند باعث overfitting شود ([Bailey et al., The Probability of Backtest Overfitting](https://www.davidhbailey.com/dhbpapers/backtest-prob.pdf)). برچسب ACCEPT در Auto فقط مرحلهٔ انتخاب است؛ برای نتیجهٔ قابل اتکا، Research و sealed holdout مستقل لازم‌اند. اسکریپت بالا اجرای Auto را آماده می‌کند و دادهٔ holdout را به مدل یادگیری وارد نمی‌کند.
