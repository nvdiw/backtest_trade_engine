# خروجی مشترک و Optimize ساده

برای اجرای جدید، نام استراتژی کافی است. Pulse دادهٔ 1m و MA دادهٔ 15m خودش را انتخاب می‌کند؛ فاصلهٔ کندل‌ها از داده بررسی می‌شود.

```powershell
python optimize.py pulse --cycles 200
python optimize.py ma --cycles 200
python optimize.py status pulse_quality_200_20260908_100013
python optimize.py report pulse_quality_200_20260908_100013
python optimize.py resume pulse_quality_200_20260908_100013 --cycles 50
```

در resume تعداد cycle، تعداد **اضافی** است. نیازی به تکرار base-source، base-params یا output-dir نیست. اگر نام پوشه تکراری باشد مسیر کامل بدهید. فرمان report فقط از ارزیابی‌های ذخیره‌شده گزارش می‌سازد و تست را دوباره اجرا نمی‌کند. Snapshot پیش‌فرض هر ۵۰ cycle است.

اسکریپت آمادهٔ PowerShell هم از همین رابط استفاده می‌کند و Python را در همان ترمینال اجرا می‌کند:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\run_pulse_long_test.ps1 -Cycles 200 -Workers 8
powershell -NoProfile -ExecutionPolicy Bypass -File .\run_pulse_long_test.ps1 -Resume pulse_quality_200_20260908_100013 -Cycles 50
```

## مسیر مشترک گزارش‌ها

استراتژی فقط منطق معاملات و تنظیمات DATA_FILE، TIMEFRAME، DISPLAY_NAME و در صورت نیاز SYMBOL را مشخص می‌کند. TIMEFRAME می‌تواند auto باشد. برای دادهٔ دیگر، `--data-file` و در صورت نیاز `--symbol ETHUSDT` قابل استفاده‌اند؛ نام نماد در نبود تنظیم صریح از بخش اول نام فایل گرفته می‌شود.

TradeEngine محاسبات حساب را به `run_reporting.py` تحویل می‌دهد. همین ناشر مشترک، CSVlogger و chart renderer را برای همهٔ استراتژی‌ها فراخوانی می‌کند. منطق حسابداری موجود استراتژی‌ها حفظ شده؛ کد انتشار گزارش شاخهٔ جداگانهٔ 1m و 15m ندارد.

خروجی بک‌تست با ثبت معاملات فعال شامل result.json، params.json، run_summary.csv، run.log، فایل معاملات CSV/XLSX، خلاصهٔ ماهانه و گزارش رسیدن به هدف ماهانه است. نمودار از مسیر مشترک ساخته می‌شود؛ برای ذخیرهٔ تصویر `--chart-file` بدهید. آمار مدت تست، ماه‌های بررسی‌شده و ماه‌های موفق/ناموفق در گزارش ماهانه قرار دارد. اجرای سریع داخلی optimize همچنان از نوشتن فایل معاملات برای تک‌تک ارزیابی‌ها صرف‌نظر می‌کند؛ گزارش کمپین و خروجی نامزدهای انتخاب‌شده مسیر مشترک دارند.

گزارش کمپین شامل campaign_report.xlsx، auto_report.xlsx، campaign_status.csv، stage_summary.csv، stage_observations.csv، campaign.log و CAMPAIGN.md است. وضعیت و نامزدهای تأییدشده از مشاهدات موقت جدا هستند. حتی با صفر نامزد نهایی، گزارش وضعیت و علت رد شدن‌ها ساخته می‌شود.

## نتیجهٔ اجرای قبلی ۲۰۰ cycle

پوشهٔ `pulse_quality_200_20260908_100013` کامل شده است: ۲۰۰ cycle و ۳۰٬۴۲۸ ارزیابی. هیچ نامزدی همهٔ مراحل پذیرش را با موفقیت نگذرانده است. گزارش‌های وضعیت و snapshotهای خالی از داده‌های ذخیره‌شده بازسازی شده‌اند.

در Validation از ۴۸۰۰ ارزیابی، ۴۷۹۳ مورد کمتر از حداقل ۳۰ معامله داشتند. این حد ثابت برای Discovery حدود ۹ماهه و Validation حدود ۳ماهه یکسان بود. هفت نامزد مرحلهٔ stress هم بازده خالص مثبت نداشتند؛ بنابراین نمی‌توان پارامتر checkpoint را برندهٔ تأییدشده دانست.

در اجرای جدید با فرمان کوتاه Pulse، پروفایل quality و یادگیری profit-evidence انتخاب می‌شود و حداقل معاملات متناسب با طول هر مرحله محاسبه می‌شود. مثلاً حداقل ۳۰ معامله در ۹ ماه، برای ۳ ماه به ۱۰ تبدیل می‌شود. این کار تناسب مراحل را اصلاح می‌کند؛ سود بیشتر را تضمین نمی‌کند. Resume کمپین قدیمی همان معیار ثابت قبلی را نگه می‌دارد تا نتایج با معیارهای متفاوت مخلوط نشوند. رابط قدیمی آرگومان‌ها نیز سازگار باقی مانده است.

تست یکپارچگی، مجموعهٔ فایل‌ها، ستون‌های CSV، sheetهای Excel و ساخت نمودار را برای Pulse/BTC/1m، MA/BTC/15m و Pulse/ETH/5m بررسی می‌کند.
